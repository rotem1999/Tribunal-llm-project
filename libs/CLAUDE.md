# libs/

Shared, **framework-free** libraries imported by both apps — the point of the monorepo (one source of truth, no front/back drift). Only truly shared contract types belong here (§3.1). Currently: `shared-types`.
