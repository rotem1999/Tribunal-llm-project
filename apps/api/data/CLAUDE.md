# data/

Generated run output — the run files here are **gitignored**, not source. The economy writer drops per-run `runs/<runId>.json` here and appends `ledger.jsonl` (§6); both are reconstructable from the DB, so don't hand-edit or commit them, and don't add code here.

**This `CLAUDE.md` is tracked, not ignored.** Ignore run output by file (`data/runs/`, `data/ledger.jsonl`), never the whole `data/` folder — git can't re-include a file inside a fully-ignored directory.
