# apps/web-e2e/

Optional Playwright happy-path e2e (§14.5), backend mocked/seeded: login -> run (Mode A) -> see 4 speeches, 3 verdicts, the non-binding tally, the economy panel, and a working Download JSON. Run via `nx e2e web-e2e`.
